# Ticket: 07 — 结构化结果契约（resolve 两步协议收拢）

- **日期**: 2026-08-24
- **来源**: 父 issue「FFI 层 52 个直通导出加深为结构化契约——错误随结果返回、状态走事件、绑定生成」（#166）的 tracer-bullet 切片
- **状态**: 已立项（GitHub issue #176）
- **涉及平台**: 双端 + Rust 核心

---

## Parent
FFI 层 52 个直通导出加深为结构化契约——错误随结果返回、状态走事件、绑定生成（#166）

## What to build
定义统一结果类型（成功载荷 + 分类错误一次返回）；先落地到解析调用：原先「返回 null 再查全局错误槽」的两步协议收拢为一次返回，全局槽不再被该调用使用。

## Acceptance criteria
- [ ] 解析调用一次返回结果或分类错误
- [ ] 该调用不再读取全局错误槽
- [ ] 契约形状测试覆盖成功与各类失败

## Blocked by
None — can start immediately

## Coordination
结果类型同时供协调器新导出与「播放期解析策略」组（#168 组）使用
