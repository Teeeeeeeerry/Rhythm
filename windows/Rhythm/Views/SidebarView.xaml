<UserControl
    x:Class="Rhythm.Views.SidebarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ListView x:Name="sidebarList" SelectionChanged="OnSelectionChanged">
        <ListViewItem Tag="Library">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <SymbolIcon Symbol="MusicInfo" />
                <TextBlock Text="资料库" />
            </StackPanel>
        </ListViewItem>
        <ListViewItem Tag="Playlists">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <SymbolIcon Symbol="List" />
                <TextBlock Text="播放列表" />
            </StackPanel>
        </ListViewItem>
    </ListView>
</UserControl>
