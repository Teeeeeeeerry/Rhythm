# Rhythm

跨平台（macOS/Windows）音乐播放器：Rust 核心（`rust-core/`）+ SwiftUI 前端（`macos/`）+ WinUI 3 前端（`windows/`）。

## Language

**行为清单**：
按模块维护的行为规格文件（`docs/testing/behavior/<模块>.md`），每条行为分属主路径/边界情况/错误路径三层并标 P0/P1/P2 优先级；清单上每一项都必须有自动化测试，行覆盖率只作参考。
_Avoid_: 测试计划、覆盖清单

**红测禁用**：
行为清单中某项行为因产品代码存在已登记的真实缺陷而无法通过的测试：测试照写但用 `XCTSkip`/`#[ignore]` 禁用，禁用原因挂 GitHub issue 链接，缺陷修复时解禁。
_Avoid_: 跳过测试、known-bug 测试

**最小接缝**：
为解锁测试而进行的结构性改动，限定为引入 trait/protocol 注入点，与测试同批提交并注明理由；不得改变产品行为。
_Avoid_: 测试重构、mock 化

**波次**：
按依赖顺序切分的工作批次，每批产出可独立合并的最小接缝与行为测试，对应 `/to-tickets` 拆出的一张或多张票。
_Avoid_: 里程碑、阶段

**清单条目优先级**：
P0 = 主路径与历史回归行为，是该模块票的合并门槛；P1 = 边界情况，同波次内完成；P2 = 错误路径，仅断言"错误被正确上报"，可顺延至后续波次。
_Avoid_: must-have、nice-to-have
